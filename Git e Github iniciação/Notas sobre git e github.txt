Sha1 = a encriptação gera um conjunto de caracteres identificados em 40 dígitos/forma curta de representar
um arquivo. 

Blobs = bloco básico, conserva metadados (não guarda nome, só guarda o sha1)

Tree = armazena blobs

Commit = tudo 

Chave SSH = entre duas máquinas